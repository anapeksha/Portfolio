import ContactBar from "../components/ContactBar";
import {Box} from "@mui/material"

const Contact = () => {
	return (
		<Box id="contact">
			<ContactBar />
		</Box>
	);
};

export default Contact;
